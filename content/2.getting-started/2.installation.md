---
title: Installation
description: ''
position: 2.1
category: Installation
---

Once you've completed all the prerequisites, you can go ahead and start to install Postal.

## Configuration

Before you can start Postal, you'll need some configuration. The repository you cloned includes a tool to automatically generate some initial configuration files.

The hostname used by the Postal installation is separate from the domains that Postal will send mail for. In these examples, we'll use `postal.example.com` as the Postal hostname, matching the example used by the default Postal configuration. You may use another stable hostname, such as `mail.example.com`, but `postal.example.com` remains the canonical documentation example. Keep the roles separate: changing the Postal hostname does not change the sending domain or any domain-derived DNS records.

::callout{icon="i-heroicons-exclamation-triangle" color="amber"}
**Important:** The `postal` label in `postal.example.com` is only part of the example hostname. It is not a reference to `dns.dkim_identifier`, `dns.domain_verify_prefix`, or `dns.custom_return_path_prefix`. Those configuration values have separate meanings and must not be inferred from the hostname.
::

Run the command below and replace `postal.example.com` with the actual hostname you want to access your Postal web interface at. Make sure you have set up this hostname with your DNS provider before continuing.

```bash
postal bootstrap postal.example.com
```

This will generate three files in `/opt/postal/config`.
* `postal.yml` is the main postal configuration file
* `signing.key` is the private key used to sign various things in Postal
* `Caddyfile` is the configuration for the Caddy webserver

Once generated, open `/opt/postal/config/postal.yml` and set the values appropriate for your installation, including database credentials and other deployment-specific settings.

::callout{icon="i-heroicons-information-circle"}
Note that the Docker setup mounts <code>/opt/postal/config</code> as <code>/config</code> so any full directory paths mentioned in <code>postal.yml</code> will likely need to start with <code>/config</code> and not <code>/opt/postal/config</code>.
::

## Configuration file reference

Postal 3.x uses a version 2 YAML configuration file, normally stored at `/opt/postal/config/postal.yml`. The complete configuration schema is maintained in the Postal source repository at [`doc/config/yaml.yml`](https://github.com/postalserver/postal/blob/main/doc/config/yaml.yml). Environment variables are also supported for the same configuration options. If you change the configuration, restart Postal before expecting the new values to be used. See the [configuration reference](https://docs.postalserver.io/getting-started/configuration/) for the generated/default configuration details.
The sections below mirror every top-level section and parameter currently present in `doc/config/yaml.yml`. The source file remains the authoritative reference for defaults and comments; this section explains the operational role of each parameter and highlights values that are commonly changed during deployment. The corresponding environment-variable names are documented separately in [`doc/config/environment-variables.md`](https://github.com/postalserver/postal/blob/main/doc/config/environment-variables.md). The current Postal 3.x configuration uses YAML only; do not look for a TOML equivalent. Keep this reference synchronized with the source configuration file when upgrading Postal.
::callout{icon="i-heroicons-information-circle"}
**Naming rule:** `postal.example.com` is the canonical Postal hostname used in this documentation. You may choose another hostname, such as `mail.example.com`, but do not infer the meanings of `dkim_identifier`, `domain_verify_prefix`, or `custom_return_path_prefix` from the word `postal` in the hostname. These settings control separate DNS behavior.
::
### `version`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `version` | Selects the configuration format. | Use `2` for the current Postal 3.x configuration. |

### `postal`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `web_hostname` | Public hostname used by the Postal web interface. | DNS hostname such as `postal.example.com`. |
| `web_protocol` | Protocol used for the Postal web interface. | Usually `https`; `http` is generally only appropriate for local/non-production setups. |
| `smtp_hostname` | Hostname of the Postal SMTP server. | Usually the public Postal SMTP hostname, for example `postal.example.com`. |
| `use_ip_pools` | Enables Postal IP pool functionality. | `true` or `false`. |
| `default_maximum_delivery_attempts` | Default maximum delivery attempts for messages. | Positive integer; default `18`. |
| `default_maximum_hold_expiry_days` | Default number of days a held message is retained. | Positive integer; default `7`. |
| `default_suppression_list_automatic_removal_days` | Default suppression-list retention period. | Positive integer; default `30`. |
| `default_spam_threshold` | Default spam score threshold. | Integer; default `5`. |
| `default_spam_failure_threshold` | Default spam-failure threshold. | Integer; default `20`. |
| `use_local_ns_for_domain_verification` | Uses local/system nameservers for domain verification and DNS checks instead of resolving through the domain's nameserver. | `true` or `false`; default `false`. Useful for diagnosing resolver/environment issues. |
| `use_resent_sender_header` | Adds a `Resent-Sender` header to outgoing mail. | `true` or `false`. |
| `signing_key_path` | Private key used by Postal for its internal signing operations. | File path; keep the generated private key protected. |
| `smtp_relays` | List of outbound SMTP relay URLs. | Array of URLs such as `smtp://host:port`; leave empty when Postal delivers directly. |
| `trusted_proxies` | Additional trusted proxy addresses. | Array of IP addresses; add only actual trusted proxy addresses. |
| `allowed_request_destinations` | Exceptions for outbound webhook/HTTP destinations that resolve to reserved addresses. | Array of hostnames or IP/CIDR ranges; keep empty unless a trusted destination genuinely requires an exception. |
| `queued_message_lock_stale_days` | Age after which a queued-message lock is treated as stale. | Positive integer; default `1`. |
| `batch_queued_messages` | Dequeues queued messages in destination-based batches. | `true` or `false`; default `true`. |

### `web_server`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `default_port` | Default HTTP port for the Postal web server. | Default `5000`; can also be overridden by `PORT`. |
| `default_bind_address` | Default bind address for the web server. | Default `127.0.0.1`; use a broader address only when your proxy/network design requires it. |
| `max_threads` | Maximum web-server threads. | Positive integer; default `5`. |

### `worker`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `default_health_server_port` | Worker health endpoint port. | Default `9090`. |
| `default_health_server_bind_address` | Bind address for the worker health server. | Default `127.0.0.1`. |
| `threads` | Number of threads used by each worker. | Positive integer; default `2`. |

### `main_db`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `host` | Main MariaDB hostname/address. | `localhost`, private DNS name, or private IP. |
| `port` | Main MariaDB port. | Default `3306`. |
| `username` | MariaDB username for the main Postal database. | String. |
| `password` | MariaDB password for the main Postal database. | Secret; do not commit it to source control. |
| `database` | Main Postal database name. | Default `postal`. |
| `pool_size` | Maximum MariaDB connection-pool size. | Positive integer; default `5`. |
| `encoding` | MariaDB connection encoding. | Default `utf8mb4`. |

### `message_db`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `host` | MariaDB host for mail-server message databases. | Same or different private DB host. |
| `port` | MariaDB port for message databases. | Default `3306`. |
| `username` | MariaDB username for message databases. | String. |
| `password` | MariaDB password for message databases. | Secret. |
| `encoding` | MariaDB connection encoding. | Default `utf8mb4`. |
| `database_name_prefix` | Prefix used for Postal message database names. | Default `postal`; changing it affects database naming and migration/backup procedures. |

### `logging`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `rails_log_enabled` | Enables the default Rails logger. | `true` or `false`; default `false`. |
| `sentry_dsn` | Sends application exception reports to Sentry. | Sentry DSN or empty. |
| `enabled` | Enables Postal's logger to STDOUT. | `true` or `false`; default `true`. |
| `level` | Minimum Postal log level. | `debug`, `info`, `warn`, `error`, or `fatal`. Default is `INFO`. |
| `highlighting_enabled` | Enables highlighted log output. | `true` or `false`. |

### `gelf`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `host` | GELF log destination. | Hostname/IP; leave empty if unused. |
| `port` | GELF port. | Default `12201`. |
| `facility` | GELF facility value. | Default `postal`. |

### `smtp_server`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `default_port` | Port on which Postal's SMTP server listens. | Default `25`; can also be overridden by `PORT`. |
| `default_bind_address` | Bind address for Postal SMTP. | Default `::`; use a specific address when required by your network design. |
| `default_health_server_port` | SMTP health endpoint port. | Default `9091`. |
| `default_health_server_bind_address` | Bind address for SMTP health endpoint. | Default `127.0.0.1`. |
| `tls_enabled` | Enables TLS on the SMTP listener. | `true` or `false`; requires certificate/key. |
| `tls_certificate_path` | SMTP TLS certificate path. | Default `$config-file-root/smtp.cert`; required when TLS is enabled. |
| `tls_private_key_path` | SMTP TLS private-key path. | Default `$config-file-root/smtp.key`; keep private. |
| `tls_ciphers` | Overrides OpenSSL cipher selection. | Empty for defaults or a provider-approved cipher configuration. |
| `ssl_version` | Supported SSL/TLS version setting. | Default `SSLv23` in the current sample; review against your OpenSSL/runtime requirements before overriding. |
| `proxy_protocol` | Enables Proxy Protocol v1 support. | `true` or `false`; enable only when the upstream load balancer actually sends it. |
| `log_connections` | Logs SMTP connections. | `true` or `false`. |
| `max_message_size` | Maximum accepted SMTP message size in MB. | Positive integer; default `14`. |
| `log_ip_address_exclusion_matcher` | Regex for excluding IPs from connection logs. | Regex or empty. |

::callout{icon="i-heroicons-information-circle"}
The `smtp_server.tls_*` settings control the Postal SMTP listener. They are separate from the `smtp.*` settings, which configure the SMTP connection used for Postal-generated application emails. Do not treat changing one section as changing the other.
::

### `dns`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `mx_records` | MX hostnames Postal expects during DNS checks. | One or more hostnames. Single-node deployments may use one hostname. |
| `spf_include` | SPF hostname that sending domains should include. | Postal infrastructure hostname such as `spf.postal.example.com`. |
| `return_path_domain` | Postal Return Path infrastructure hostname. | Example `rp.postal.example.com`. |
| `route_domain` | Domain used for route-specific addresses. | Example `routes.postal.example.com`; optional if routing is unused. |
| `track_domain` | Postal tracking hostname that sender tracking domains should point to. | Example `track.postal.example.com`; optional if tracking is unused. |
| `helo_hostname` | Hostname Postal uses in outbound SMTP `HELO`/`EHLO`. | Stable FQDN; if empty, Postal uses its configured behavior/defaults. |
| `dkim_identifier` | Prefix used when Postal generates DKIM selectors for domain DNS records. | Default `postal`; custom value such as `dkim` is valid. This is independent from the Postal hostname. |
| `domain_verify_prefix` | Prefix added to Postal's domain-verification TXT value. | Default `postal-verification`; this is part of the TXT **value**, not a DNS hostname. |
| `custom_return_path_prefix` | Prefix used to derive the per-sending-domain Return Path hostname. | Default `psrp`; e.g. `psrp.example.com`. |
| `timeout` | DNS resolution timeout. | Seconds; default `5`. |
| `resolv_conf_path` | Path to the system resolver configuration used when local nameservers are selected. | Usually `/etc/resolv.conf`. |

### `smtp`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `host` | SMTP host used for Postal application-level emails. | Hostname/IP; default `127.0.0.1`. |
| `port` | Port for application-level SMTP. | Default `25`. |
| `username` | SMTP authentication username for application-level mail. | String or empty. |
| `password` | SMTP authentication password. | Secret or empty. |
| `authentication_type` | Authentication mechanism. | Default `login`; use a mechanism supported by the configured server. |
| `enable_starttls` | Requires STARTTLS and fails if unsupported. | `true` or `false`. |
| `enable_starttls_auto` | Negotiates STARTTLS automatically when offered. | `true` or `false`; default `true`. |
| `openssl_verify_mode` | Certificate verification mode for TLS. | Default `peer`; `none` disables certificate checking and should not be used in hardened production deployments. |
| `from_name` | Display name for Postal-generated application email. | String; default `Postal`. |
| `from_address` | Address for Postal-generated application email. | Valid address on a domain you control. |

### `rails`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `environment` | Rails runtime environment. | Default `production`. |
| `secret_key` | Secret used for signing/encrypting Rails cookies and session data. | Secret value; required for a secure installation and must remain private. |

### `rspamd`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `enabled` | Enables Rspamd message inspection. | `true` or `false`. |
| `host` | Rspamd host. | Default `127.0.0.1`. |
| `port` | Rspamd port. | Default `11334`. |
| `ssl` | Enables SSL for the Rspamd connection. | `true` or `false`. |
| `password` | Rspamd password. | Secret or empty. |
| `flags` | Additional Rspamd flags. | String or empty. |

### `spamd`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `enabled` | Enables SpamAssassin message inspection. | `true` or `false`. |
| `host` | SpamAssassin host. | Default `127.0.0.1`. |
| `port` | SpamAssassin port. | Default `783`. |

### `clamav`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `enabled` | Enables ClamAV message inspection. | `true` or `false`. |
| `host` | ClamAV host. | Default `127.0.0.1`. |
| `port` | ClamAV port. | Default `2000`. |

### `smtp_client`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `open_timeout` | Timeout while opening an outbound SMTP connection. | Seconds; default `30`. |
| `read_timeout` | Timeout while reading from an outbound SMTP connection. | Seconds; default `30`. |

### `migration_waiter`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `enabled` | Waits for all migrations before starting a process. | `true` or `false`; default `false`. |
| `attempts` | Number of migration checks before giving up. | Positive integer; default `120`. |
| `sleep_time` | Seconds between migration checks. | Positive integer; default `2`. |

### `oidc`
| Parameter | Purpose | Typical values / notes |
| --- | --- | --- |
| `enabled` | Enables OpenID Connect authentication. | `true` or `false`. |
| `local_authentication_enabled` | Keeps local password login available when OIDC is enabled. | `true` or `false`. Set to `false` only when local authentication should be disabled. |
| `name` | Provider name displayed in the UI. | String. |
| `issuer` | OIDC issuer URL. | HTTPS URL. |
| `identifier` | OIDC client ID. | String. |
| `secret` | OIDC client secret. | Secret. |
| `scopes` | OIDC scopes requested from the provider. | Array; defaults include `openid` and `email`. |
| `uid_field` | OIDC claim used as the user's stable UID. | Default `sub`. |
| `email_address_field` | OIDC claim containing the user's email address. | Default `email`. |
| `name_field` | OIDC claim containing the user's display name. | Default `name`. |
| `discovery` | Uses OIDC discovery metadata from the issuer. | `true` or `false`; default `true`. |
| `authorization_endpoint` | Explicit authorization endpoint when discovery is disabled. | HTTPS URL; used when `discovery: false`. |
| `token_endpoint` | Explicit token endpoint when discovery is disabled. | HTTPS URL; used when `discovery: false`. |
| `userinfo_endpoint` | Explicit UserInfo endpoint when discovery is disabled. | HTTPS URL; used when `discovery: false`. |
| `jwks_uri` | Explicit JWKS endpoint when discovery is disabled. | HTTPS URL; used when `discovery: false`. |
::callout{icon="i-heroicons-exclamation-triangle" color="amber"}
**Configuration safety:** Treat Postal hostnames and DNS prefixes as part of the configuration contract. `postal.example.com` is only the canonical example hostname; `spf.postal.example.com`, `rp.postal.example.com`, `routes.postal.example.com`, and `track.postal.example.com` are separate values configured independently. Likewise, `dkim_identifier`, `domain_verify_prefix`, and `custom_return_path_prefix` are not derived from the hostname. Do not move DNS records to a different level or rename these values simply because another name looks cleaner. Make such changes only as a coordinated DNS and configuration migration.
::

## Initializing the database

Once you've added your configuration, you need to initialize your database by adding all the appropriate tables. Run the following commands to create the schema and then create your first admin user.

```bash
postal initialize
postal make-user
```

## Running Postal

You're now ready to actually run Postal itself. You can go ahead and do this by running:

```bash
postal start
```

This will run a number of containers on your machine. You can use `postal status` to see details of these components.

## Caddy

To handle SSL termination and all web traffic, you'll need to configure a web proxy. You can use anything that takes your fancy here - nginx, Apache, HAProxy, anything - but in this example, we're going to use [Caddy](https://caddyserver.com). It's a great little server that requires very little configuration and is very easy to set up.

```
docker run -d \
   --name postal-caddy \
   --restart always \
   --network host \
   -v /opt/postal/config/Caddyfile:/etc/caddy/Caddyfile \
   -v /opt/postal/caddy-data:/data \
   caddy
```

Once this has started, Caddy will issue an SSL certificate for your domain and you'll be able to immediately access the Postal web interface and login with the user you created in one of the previous steps.

![Image](/screenshots/Screen-Shot-2021-07-29-23-26-18.23-Qwv2DD40v4jMEoaHtE.png)

## Next steps

After the Postal installation is working, configure the Postal infrastructure DNS records and then configure the sending domains that will be used by your Organizations and Mail Servers.

- [Configuration](configuration)
- [DNS configuration](dns-configuration)
- [Sending domain configuration](sending-domain-configuration)
